#include <iostream>
using nsmespace std;
int main()
{
    int array[] = {1,2,3,4};
    int total = 0;
    for(int i = 0; i < sizeof(aeeay)/sizeo(int); i++ )
    {
        total += array[i];
    }
    printf("%d",total);
}